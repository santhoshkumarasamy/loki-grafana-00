loki:
  auth_enabled: false
  storage:
    type: filesystem
  useTestSchema: true
  commonConfig:
    replication_factor: 1
backend:
  replicas: 0
read:
  replicas: 0
write:
  replicas: 0 
singleBinary:
  replicas: 1